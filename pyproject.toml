[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "focuslog"
version = "0.1.0"
description = "Help focus"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
where = ["src"]